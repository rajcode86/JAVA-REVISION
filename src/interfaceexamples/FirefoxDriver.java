package interfaceexamples;
     
      /*create new class named firefox driver then we we want inherit webdriver interface then keyword used is 'implements'.
       * 
 * if we inherit interface to interface then use keyword 'extends'.
 * multiple inheritance cannot possible on class level but possible on interface level.
 *  after making an interface named webdriver we made class named firefox driver with main method.we cannot make an object of webdriver.
 *  then use keyword 'implments' to inherit webdriver interface.
 */

public class FirefoxDriver implements WebDriver {

		@Override
		public void click() {
			// TODO Auto-generated method stub
			
		}

		@Override
		public void sendKeys() {
			// TODO Auto-generated method stub
			
		}

	
	}


