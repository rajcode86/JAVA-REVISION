package polymorphism;

/*Overriding/dynamic binding/late binding/run time polymorphism
 * overriding is different classes(with inheritance applied)and where we have methods with same signatures.
 * overriding is changing the inner content of that file.file will remain same.
 * Can we change return type while performing overriding?-->we cannot,when the return types are primitive. and 
 * we can as well depends on condition when return types are class name.
 */

public class Overriding_Poly {

}
