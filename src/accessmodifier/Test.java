package accessmodifier;

/* Access modifier specifies access level of a java component. these are two types: Class level and member level.
 * class can be only public and default. we can not make it protected.
 * Class member level access modifiers can be public,protected,default,private.
 * public --> if a member marked as public it will be accessible in whole java world.
 * default -->if member has no modifier it will be marked as'default' implicitly and accessible in its package only.
 * protected--> if member is protected then it is accessible in its package only. it will accessible outside the package through'inheritance' 
 * private-->it will accessible in its class only if member is marked as private.
 * 
 * 
 */

public class Test {

	public static void main(String[] args) {
	

	}

}
